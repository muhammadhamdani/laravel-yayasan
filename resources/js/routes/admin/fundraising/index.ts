import rekeningProviders from './rekening-providers'
import rekenings from './rekenings'
const fundraising = {
    rekeningProviders: Object.assign(rekeningProviders, rekeningProviders),
rekenings: Object.assign(rekenings, rekenings),
}

export default fundraising