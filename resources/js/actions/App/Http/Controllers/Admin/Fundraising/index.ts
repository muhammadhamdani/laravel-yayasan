import RekeningProviderController from './RekeningProviderController'
import RekeningController from './RekeningController'
const Fundraising = {
    RekeningProviderController: Object.assign(RekeningProviderController, RekeningProviderController),
RekeningController: Object.assign(RekeningController, RekeningController),
}

export default Fundraising